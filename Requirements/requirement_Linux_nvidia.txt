# Requirements for OCR_with_camera_UI_ver1.py on Linux with NVIDIA GPU (CUDA)
# These wheels include the CUDA runtime via the PyTorch index.
# If install fails due to a CUDA/driver mismatch, change cu121 -> a driver-compatible build (e.g., cu118),
# or remove the extra-index line to fall back to CPU wheels.
--extra-index-url https://download.pytorch.org/whl/cu121

# Core deep learning stack (CUDA)
torch
torchvision

# Model + utilities
transformers
safetensors
timm
einops
sentencepiece

# Optional speedups (Linux + CUDA): FlashAttention2
# If installation fails on your distro/CUDA combo, comment this line out.
flash-attn

# App + image I/O + UI
openai
pygame
pillow