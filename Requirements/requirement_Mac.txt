# CPU-only requirements for OCR_with_camera_UI_ver1.py
# Use this on macOS or any machine without NVIDIA CUDA.

# Core deep learning stack
torch
torchvision

# Model + utilities
transformers
timm
einops
sentencepiece

# App + image I/O + UI
openai
pygame
pillow