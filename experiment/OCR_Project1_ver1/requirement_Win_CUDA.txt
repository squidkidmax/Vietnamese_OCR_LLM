# Requirements for OCR_with_camera_UI_ver1.py on Windows with NVIDIA GPU (CUDA Toolkit installed)
# These wheels include the CUDA runtime — the local Toolkit is not required at runtime but is fine to have.
# If install fails due to a CUDA/driver mismatch, change cu121 -> a driver-compatible build (e.g., cu118),
# or remove the extra-index line to fall back to CPU wheels.
--extra-index-url https://download.pytorch.org/whl/cu121

# Core deep learning stack (GPU builds pulled from the PyTorch CUDA index above)
torch
torchvision

# Model + utilities
transformers
safetensors
timm
einops
sentencepiece

# App + image I/O + UI
openai
pygame
pillow