# Requirements for OCR_with_camera_UI_ver1.py on Windows with Integrated Graphics (Intel/AMD, no NVIDIA CUDA)
# These will install CPU-only wheels of PyTorch/TorchVision from PyPI.
# No CUDA/FlashAttention2 needed.

# Core deep learning stack (CPU)
torch
torchvision

# Model + utilities
transformers
safetensors
timm
einops
sentencepiece

# App + image I/O + UI
openai
pygame
pillow

# Optional: If you plan to use DirectML acceleration (Windows on AMD/Intel GPUs),
# install this *in addition* and update your code to use the DirectML device.
# pip install torch-directml